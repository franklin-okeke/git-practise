git and github

BRANCHING

git branch dev  - creates a branch called dev
git branch feature/contact-form  - creates a branch called feature/contact-form

NB: always pull the latest chnages before creating a branch

git checkout dev - navigates to the branch dev
git checkout -b dev  - creates and navigates inside the branch once

git branch -- shows us all the branch we r have in the project

git pull origin main - pulls the lattest chnages from the main branch
git pull origin dev - pulls the lattest chnages from the dev branch

git push origin feature/footer -- specifies the branch to push to

pull request -- tell ur teams the changes you made in your code
in ur branch so that they can merge

 git branch -d feature/footer -- deletes a branch after merged

 NB: to add collaborators -- 
 1. click on settings on that repo
 2. click collaborators on left
 3. search the person name and click invites

 WORKFLOW
 1. create a project add codes and push to main as a team lead
 2. or you are added as a collaborator to a repo(clone the repo)
 3. run git branch to know all branches (git pull to be updated)
 NB: if u r asked to work from another branch navigate to that branch
 run git pull to be updated then create a branch to make a copy
 4. git branch feature/footer to create your branch
 5. git checkout feature/footer to navigate into it
 NB: run git checkout -b feature/footer to create and navigete once
 6. you now have a copy of the main code, modify and add features
 7. git add ., commit
 8. git push origin feature/footer
 9. in github the branch will shows
 10. click compare and send pull request
 11. add title and description and send
 TEAM LEAD GETS THE PR
 1. click file changes to see what he code
 2. click conversation to return
 3. click merge and delete the branch
 4. click code when done
 WHEN DONE
 1. return to vs code
 2. git checkout main to return
 3. git pull to add lattest changes
 4. git branch -D feature/footer  to remove the branch
